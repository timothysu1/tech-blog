<section class="card">
  <header>{{title}}</header>
  <p>{{content}}</p>
  <h3>{{user.username}}</h3>

  <h3>Comments</h3>
  <div>
    {{#each comments}}

    {{/each}}
  </div>
  <div>
    <form>
      <textarea id="commment-input"></textarea>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>
</section>